{
  "builds": [
    {
      "src": "src/main/frontend/angular.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "src/main/frontend/dist/frontend"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
